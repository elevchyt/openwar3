node_modules/
dist/
*.local
.DS_Store
.vscode/
# Never commit Blizzard assets (see plan §0)
Warcraft III 1.30.4/
Warcraft III/
*.mpq
*.w3x
*.w3m
*.w3n
*.mdx
*.blp

# JASS interpreter throwaway build (tools/tsconfig.jass.json → CJS for headless tests)
.jass-build/

# Sim throwaway build (tools/tsconfig.sim.json → CJS for headless targeting tests)
.sim-build/

# Campaign index throwaway build (tools/tsconfig.campaign.json → CJS for the headless check)
.campaign-build/

# CASC reader throwaway build (tools/tsconfig.casc.json → CJS for casc:test and data:extract)
.casc-build/
